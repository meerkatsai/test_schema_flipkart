# Layer: Platform Heuristics
What is true of a PLATFORM for everyone: native labels + display traps
(dictionary), account anatomy + blast radius (structure), legal automation
surface (capabilities), driver graphs + data-quality checks + simulation levers
(diagnostics). Names are catalog keys — the semantic layer is the name authority.
- `contract_platform-{dictionary,structure,capabilities,diagnostics}-v1` — the four generic pack contracts
- `meerkats_flipkart_schema-v1.schema.json` — the consolidated Flipkart-only contract (all four packs + DM warehouse, invariants baked as consts: ROI display, alert-only campaigns, T+28, additive-only gold)
